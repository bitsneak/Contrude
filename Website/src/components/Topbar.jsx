import React from 'react'

const Topbar = () => {
  return (
    <div className='h-16 bg-blue-200'>
          {/* Top bar content (like navigation, title, etc.) */}
          <p>Top Bar</p>
    </div>
  )
}

export default Topbar